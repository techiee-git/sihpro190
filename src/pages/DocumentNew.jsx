import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Icon } from '../components/Icon';

export function DocumentNew() {
  const [form, setForm] = useState({ title: '', category: '', description: '' }); const [file, setFile] = useState(null); const [errors, setErrors] = useState({}); const [checked, setChecked] = useState(false);
  const update = event => setForm(current => ({ ...current, [event.target.name]: event.target.value })); const checkFields = event => { event.preventDefault(); const next = {}; if (!form.title.trim()) next.title = 'Enter a document title.'; if (!form.category) next.category = 'Select a document category.'; setErrors(next); setChecked(true); };
  return <div className="dashboard form-page"><div className="form-page-heading"><div><p className="eyebrow">DOCUMENTS</p><h1>Add Document</h1><p className="page-subtitle">Prototype layout for a future document workflow.</p></div><Link className="back-to-cases" to="/case-workspace/documents"><Icon name="chevronLeft" size={16} />Back to Documents</Link></div><form className="panel form-panel" onSubmit={checkFields} noValidate><div className="panel-heading"><h2>Document details</h2><p>Case association is unavailable until a real case is selected. The selected file remains local and is not uploaded.</p></div><div className="form-fields"><label>Display name<input name="title" value={form.title} onChange={update} aria-invalid={Boolean(errors.title)} />{errors.title && <span className="field-error">{errors.title}</span>}</label><label>Category<select name="category" value={form.category} onChange={update} aria-invalid={Boolean(errors.category)}><option value="">Select a category</option><option>FIR</option><option>Statements</option><option>Charge Sheet</option><option>Legal Documents</option></select>{errors.category && <span className="field-error">{errors.category}</span>}</label><label>Description <span className="optional-label">(optional)</span><textarea name="description" value={form.description} onChange={update} rows="5" /></label><label>Local file <span className="optional-label">(optional)</span><input type="file" onChange={event => setFile(event.target.files?.[0] || null)} /><span className="field-help">No file contents are read, hashed, encrypted, previewed, or persisted.</span>{file && <span className="local-file">Selected locally: {file.name} ({Math.round(file.size / 1024)} KB)</span>}</label></div><div className="form-actions"><button className="check-fields-btn" type="submit"><Icon name="checklist" size={16} />Check fields</button><Link className="cancel-link" to="/case-workspace/documents">Cancel</Link><button className="disabled-submit" disabled>Add document</button></div>{checked && <p className={`validation-message ${Object.keys(errors).length ? 'has-errors' : ''}`} role="status">{Object.keys(errors).length ? 'Check the highlighted fields.' : 'Fields are valid locally. Document upload is unavailable.'}</p>}</form></div>;
}
